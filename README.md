# SPI
This project implements a Serial Peripheral Interface (SPI) controller with an AMBA APB (Advanced Peripheral Bus) interface. The design allows easy integration of the SPI module into APB-based SoCs.
